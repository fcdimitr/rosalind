package rosalind

enum RequestStatus {

    PENDING,
    APPROVED,
    REJECTED,
    EXPIRED

    // final String val
    // RequestStatus( String val ) { this.val = val }

    // String toString() { return val }

}
